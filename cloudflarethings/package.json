{
	"name": "cloudflarethings",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "npx wrangler d1 migrations apply dse_trend  && npx wrangler deploy",
		"dev": "wrangler dev --test-scheduled",
		"start": "wrangler dev --test-scheduled",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"typescript": "^5.5.2",
		"wrangler": "^4.42.0"
	}
}